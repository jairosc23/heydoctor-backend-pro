[phases.setup]
nixPkgs = ["nodejs-20_x"]

[phases.install]
cmds = ["cd heydoctor-backend-pro && npm ci"]

[phases.build]
cmds = ["cd heydoctor-backend-pro && npm run build"]

[start]
cmd = "cd heydoctor-backend-pro && npm run start:prod"
