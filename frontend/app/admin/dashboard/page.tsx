'use client';

import { BusinessAdminDashboard } from '../../../components/BusinessAdminDashboard';

/**
 * Ruta Next.js App Router: `/admin/dashboard`.
 * Requiere app Next que monte esta carpeta `app/` y dependencias React.
 */
export default function AdminBusinessDashboardPage() {
  return <BusinessAdminDashboard />;
}
