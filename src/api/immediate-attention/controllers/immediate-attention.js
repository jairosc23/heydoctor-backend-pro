'use strict';

/**
 * immediate-attention controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::immediate-attention.immediate-attention');
